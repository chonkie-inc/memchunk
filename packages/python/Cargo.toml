[package]
name = "chonkie-core-python"
version = "0.10.1"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[lib]
name = "_chunk"
crate-type = ["cdylib"]

[features]
default = ["numpy-support"]
numpy-support = ["dep:numpy"]

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module"] }
numpy = { version = "0.27", optional = true }
chunk = { path = "../.." }
